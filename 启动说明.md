# 🚀 消息通知系统 - 启动说明

## 📝 快速启动指南（适合文员）

### 第一次使用

#### 1. 安装依赖（只需做一次）

**Windows 用户：**

双击运行 `安装依赖.bat`（需要先创建此文件，见下方）

或打开命令行（cmd）：

```bash
# 进入server目录
cd server
npm install

# 进入client目录
cd ../client
npm install
```

#### 2. 配置数据库（只需做一次）

1. 在 `server` 目录下找到 `.env` 文件（如果没有，已为您创建）
2. 用记事本或其他文本编辑器打开 `server/.env` 文件
3. 修改数据库密码：
   ```
   DB_PASSWORD=你的数据库密码
   ```
   **注意**：如果是本地开发且MySQL没有设置密码，可以留空
4. 保存文件

#### 3. 导入数据库（只需做一次）

在 MySQL 中执行 `server/config/init.sql` 文件。

### 日常启动

#### 开发环境（本地测试）

**启动后端：**
```bash
cd server
npm run dev
```
后端运行在：http://localhost:3000

**启动前端：**
```bash
cd client
npm run dev
```
前端运行在：http://localhost:5173

#### 生产环境（正式使用）

**构建前端：**
```bash
cd client
npm run build
```

**启动后端：**
```bash
cd server
npm start
```

## 🖥️ Windows 一键启动脚本

### 安装依赖.bat

创建 `安装依赖.bat` 文件，内容：

```batch
@echo off
echo 正在安装后端依赖...
cd server
call npm install
echo.

echo 正在安装前端依赖...
cd ..\client
call npm install
echo.

echo 安装完成！
pause
```

### 启动后端.bat

创建 `启动后端.bat` 文件，内容：

```batch
@echo off
title 消息通知系统 - 后端服务
cd server
npm start
```

### 启动前端开发.bat

创建 `启动前端开发.bat` 文件，内容：

```batch
@echo off
title 消息通知系统 - 前端开发
cd client
npm run dev
```

### 构建前端.bat

创建 `构建前端.bat` 文件，内容：

```batch
@echo off
echo 正在构建前端...
cd client
call npm run build
echo.
echo 构建完成！输出目录：client\dist
pause
```

### 一键启动开发环境.bat

创建 `一键启动开发环境.bat` 文件，内容：

```batch
@echo off
echo 正在启动消息通知系统...
echo.

echo 1. 启动后端服务...
start "后端服务" cmd /k "cd server && npm run dev"

timeout /t 3

echo 2. 启动前端服务...
start "前端服务" cmd /k "cd client && npm run dev"

echo.
echo 启动完成！
echo 后端地址：http://localhost:3000
echo 前端地址：http://localhost:5173
echo.
echo 请在浏览器中打开：http://localhost:5173
pause
```

## 🐧 Linux/Mac 启动脚本

### install.sh（安装依赖）

```bash
#!/bin/bash

echo "正在安装后端依赖..."
cd server
npm install

echo ""
echo "正在安装前端依赖..."
cd ../client
npm install

echo ""
echo "安装完成！"
```

使用方法：
```bash
chmod +x install.sh
./install.sh
```

### start-dev.sh（启动开发环境）

```bash
#!/bin/bash

echo "正在启动消息通知系统..."

# 启动后端
cd server
npm run dev &
BACKEND_PID=$!

# 等待3秒
sleep 3

# 启动前端
cd ../client
npm run dev &
FRONTEND_PID=$!

echo ""
echo "启动完成！"
echo "后端 PID: $BACKEND_PID"
echo "前端 PID: $FRONTEND_PID"
echo ""
echo "后端地址：http://localhost:3000"
echo "前端地址：http://localhost:5173"
echo ""
echo "按 Ctrl+C 停止服务"

wait
```

使用方法：
```bash
chmod +x start-dev.sh
./start-dev.sh
```

### build.sh（构建生产版本）

```bash
#!/bin/bash

echo "正在构建前端..."
cd client
npm run build

echo ""
echo "构建完成！输出目录：client/dist"
```

使用方法：
```bash
chmod +x build.sh
./build.sh
```

## ⚙️ PM2 管理（推荐生产环境）

### 安装 PM2

```bash
npm install -g pm2
```

### 启动应用

```bash
# 进入项目目录
cd /path/to/notification

# 启动后端
cd server
pm2 start index.js --name notification-system

# 查看状态
pm2 status

# 查看日志
pm2 logs notification-system

# 设置开机自启
pm2 startup
pm2 save
```

### 常用 PM2 命令

```bash
# 查看所有应用
pm2 list

# 重启应用
pm2 restart notification-system

# 停止应用
pm2 stop notification-system

# 删除应用
pm2 delete notification-system

# 查看详细信息
pm2 show notification-system

# 监控
pm2 monit
```

## 🔧 故障排除

### 1. 端口被占用

**后端端口（3000）被占用：**

修改 `server/.env` 中的 `PORT=3000` 为其他端口，如 `PORT=3001`

**前端端口（5173）被占用：**

修改 `client/vite.config.js` 中的 `server.port`

### 2. 依赖安装失败

使用淘宝镜像：

```bash
npm install --registry=https://registry.npmmirror.com
```

### 3. 数据库连接失败

检查：
1. MySQL 服务是否启动
2. `server/.env` 中的数据库配置是否正确
3. 数据库是否已导入初始化脚本

### 4. 前端打包失败

清除缓存重试：

```bash
cd client
rm -rf node_modules
rm package-lock.json
npm install
npm run build
```

## 📞 获取帮助

如遇到问题：

1. 查看后端日志：`server/` 目录下的日志文件
2. 查看浏览器控制台：按 F12 查看错误信息
3. 查看 PM2 日志：`pm2 logs`
4. 联系技术支持

---

**祝使用愉快！** 🎉

